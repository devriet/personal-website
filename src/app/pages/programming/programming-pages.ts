import { PageLink } from '../../objects/page-link';
import { ArticleReference } from 'src/app/objects/article-reference';
import { ARTICLE_REFERENCES } from '../article-view/article-references';

export const PROGRAMMING_PAGES: string[] = [
    'sticky-footer',
    'dynamic-components',
    'dark-theme',
    'iframe-nested-website'
];